---
phase: 01-ui-redesign
plan: 04
type: execute
wave: 3
depends_on: [01-01]
files_modified: [templates/public/directory.html]
autonomous: true
requirements: [UI-05]
must_haves:
  truths:
    - "Search bar is pill-shaped and transforms its icon (🔍 to 🐾) on focus"
    - "Filter chips are horizontally scrollable and use the .tag-pill style"
    - "Animal Cards implement the spring-lift effect and Karma pop animation"
  artifacts:
    - path: "templates/public/directory.html"
      provides: "Redesigned Animal Directory"
---

<objective>
Redesign the Animal Directory to improve searchability and tactile engagement using the Pawprint Studio card system and interactive filters.

Purpose: Allow users to find and interact with animals efficiently using a mobile-first, tactile interface.
Output: Redesigned Animal Directory with interactive filters and cards.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@templates/public/directory.html
@design3.md
@RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Search Bar and Filter Chips</name>
  <files>templates/public/directory.html</files>
  <action>
    - Update the search input to be a full-width pill shape (.form-input + .rounded-pill).
    - Implement the icon transformation logic (🔍 to 🐾) using Alpine.js or CSS :focus-within.
    - Implement horizontally scrollable filter chips for sectors and species as specified in design3.md (Section 6 - Directory).
    - Active filters should fill with --color-paw.
  </action>
  <verify>
    <automated>grep -q "🔍" templates/public/directory.html && grep -q "tag-pill" templates/public/directory.html</automated>
  </verify>
  <done>Search and filters are redesigned for a mobile-first experience.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Animal Cards with Spring-Lift and Karma-Pop</name>
  <files>templates/public/directory.html</files>
  <behavior>
    - Cards should have a springy hover effect: transform: translateY(-6px) scale(1.015).
    - Karma pills (personality tags) should have a 'pop' animation on tap.
    - Cards should display: photo (BlurHash), mood badge, name, fed-ago pill, and personality pills.
  </behavior>
  <action>
    - Replace existing animal cards with the .animal-card structure from design3.md (Section 4.1).
    - Use Alpine.js for the sendKarma() pop animation and heart burst (if feasible, otherwise focus on the pop).
    - Map animal mood to the appropriate badge colors (Section 4.5).
    - Use the .tag-pill style for personality traits.
  </action>
  <verify>
    <automated>grep -q "animal-card" templates/public/directory.html && grep -q "karma-pop" templates/public/directory.html</automated>
  </verify>
  <done>Animal cards are interactive and visually aligned with Pawprint Studio.</done>
</task>

</tasks>

<verification>
1. Go to the Directory page.
2. Focus the search bar and verify the icon changes to a paw.
3. Scroll the filter chips horizontally and toggle one.
4. Hover over an animal card and verify the "spring lift" effect.
5. Tap a personality tag and observe the "karma pop" animation.
</verification>

<success_criteria>
- Search and filters match the design spec.
- Animal cards are tactile and respond to hover/tap as specified.
- Grid layout is responsive and enters with staggered animations.
</success_criteria>

<output>
After completion, create .planning/phases/01-ui-redesign/01-04-SUMMARY.md
</output>
