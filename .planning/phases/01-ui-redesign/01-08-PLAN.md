---
phase: 01-ui-redesign
plan: 08
type: execute
wave: 5
depends_on: [01-03, 01-04, 01-05, 01-06, 01-07]
files_modified: [templates/base.html]
autonomous: true
requirements: [UI-10]
must_haves:
  truths:
    - "Feeding success triggers Paw Confetti (🐾, 🍚, 💛)"
    - "All interactive elements pass the 48px touch target check"
    - "Motion respects the prefers-reduced-motion media query"
  artifacts:
    - path: "templates/base.html"
      provides: "Final motion and accessibility polish"
---

<objective>
Apply final motion effects and accessibility polish to the Pawprint Studio redesign, ensuring a delightful and inclusive experience.

Purpose: Add the "celebration" layer to successful interactions and ensure the app remains usable for all.
Output: Finalized base template with motion logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@templates/base.html
@design3.md
@RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Paw Confetti and Celebration Logic</name>
  <files>templates/base.html</files>
  <action>
    - Update the celebrate() function in Alpine.store('app') to use the "Paw Confetti" particle set (🐾, 🍚, 💛, 🌿, ✨).
    - Use the 'text' shape option in canvas-confetti as specified in design3.md (Section 5).
    - Add a fallback standard confetti burst using the meadow and paw color palette.
  </action>
  <verify>
    <automated>grep -q "🐾" templates/base.html</automated>
  </verify>
  <done>Celebration effects use themed paw-print particles.</done>
</task>

<task type="auto">
  <name>Task 2: Final Accessibility and Performance Polish</name>
  <files>templates/base.html</files>
  <action>
    - Implement the @media (prefers-reduced-motion: reduce) CSS block to disable animations for users who opt-out.
    - Ensure all buttons and links meet the 48x48px touch target requirement.
    - Verify that all badges and status indicators include text labels (not just color) for color-blind accessibility.
    - Perform a final pass on focus rings to ensure they use var(--color-paw) and are clearly visible.
  </action>
  <verify>
    <automated>grep -q "prefers-reduced-motion" templates/base.html</automated>
  </verify>
  <done>Application meets Pawprint Studio accessibility and performance standards.</done>
</task>

</tasks>

<verification>
1. Trigger a feeding success (or call Alpine.store('app').celebrate() manually).
2. Verify paw emojis appear in the confetti burst.
3. Test the app with "Reduce Motion" enabled in system settings.
4. Verify all buttons are easy to tap on mobile.
</verification>

<success_criteria>
- Confetti particles are themed (paws).
- Motion is accessible and respects user preferences.
- Touch targets and focus states are standard-compliant.
</success_criteria>

<output>
After completion, create .planning/phases/01-ui-redesign/01-08-SUMMARY.md
</output>
