---
phase: 01-ui-redesign
plan: 03
type: execute
wave: 2
depends_on: [01-02]
files_modified: [templates/public/index.html]
autonomous: true
requirements: [UI-04]
must_haves:
  truths:
    - "Feeding Status Grid shows bowl emojis (🍚, 🥣, 💔) based on hours"
    - "Critical sectors ( > 6h) have a pulsing icon animation"
    - "Popular this week carousel contains Animal Cards with springy hover effects"
    - "Cards enter with a staggered animation"
  artifacts:
    - path: "templates/public/index.html"
      provides: "Redesigned Status Grid and Trending Carousel"
---

<objective>
Complete the Home page redesign by implementing the animated Feeding Status Grid and the "Popular this week" Trending Carousel.

Purpose: Provide volunteers with immediate visual feedback on feeding needs and engage students with animal profiles.
Output: Fully redesigned Home page with animations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@templates/public.html (Correction: templates/public/index.html)
@design3.md
@RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Animated Feeding Status Bowl Grid</name>
  <files>templates/public/index.html</files>
  <action>
    - Replace the binary "fed/hungry" squares with the 3-state Food Bowl cards from design3.md (Section 4.2).
    - Logic: 
      - < 3h: 🍚 (Full Bowl), --color-meadow-light
      - 3-6h: 🥣 (Half Bowl), --color-sunflower-light
      - > 6h: 💔 (Empty Bowl), --color-rosewood-light
    - Add the .bowl-pulse animation for critical sectors (> 6h).
    - Use Alpine.js to handle the visual logic if preferred, or Jinja2 conditionals.
    - Deep-link cards to /directory?sector=X.
  </action>
  <verify>
    <automated>grep -q "bowl-icon" templates/public/index.html && grep -q "bowl-pulse" templates/public/index.html</automated>
  </verify>
  <done>Feeding status grid is interactive and follows the 3-state logic.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Trending Carousel and Staggered Animations</name>
  <files>templates/public/index.html</files>
  <action>
    - Add the "Popular this week ✨" horizontal carousel using the Animal Card component (design3.md Section 4.1).
    - Implement the staggered entry animation for cards using CSS animation-delay (design3.md Section 5).
    - Ensure Animal Cards have the "springy lift" hover effect (.animal-card:hover).
    - Use BlurHash placeholders for animal images.
  </action>
  <verify>
    <automated>grep -q "Popular this week" templates/public/index.html && grep -q "animation-delay" templates/public/index.html</automated>
  </verify>
  <done>Home page carousel is functional with staggered animations.</done>
</task>

</tasks>

<verification>
1. Visit the Home page.
2. Check if sectors show 🍚/🥣/💔 based on actual data.
3. Observe the pulsing animation on sectors with 💔.
4. Verify Animal Cards in the carousel lift up when hovered.
</verification>

<success_criteria>
- Status grid reflects 3-state logic correctly.
- Critical sectors pulse visibly.
- Trending carousel cards lift on hover and enter with a stagger.
</success_criteria>

<output>
After completion, create .planning/phases/01-ui-redesign/01-03-SUMMARY.md
</output>
