---
phase: 01-ui-redesign
plan: 05
type: execute
wave: 3
depends_on: [01-01]
files_modified: [templates/public/animal_detail.html]
autonomous: true
requirements: [UI-06]
must_haves:
  truths:
    - "Animal Profile header uses full-bleed photo with Baloo 2 overlaid name"
    - "Feeding History is a vertical timeline with 🍚 icons"
    - "Sighting Gallery uses a 2-column masonry-lite grid with BlurHash"
  artifacts:
    - path: "templates/public/animal_detail.html"
      provides: "Redesigned Animal Profile"
---

<objective>
Update the Animal Profile page to provide a warm, detailed, and visually rich experience for individual animal records.

Purpose: Foster emotional connection through rich photography and a clear, story-like feeding history.
Output: Redesigned Animal Profile page.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@templates/public/animal_detail.html
@design3.md
@RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Profile Header and Status Row</name>
  <files>templates/public/animal_detail.html</files>
  <action>
    - Implement the full-bleed photo header with animal name in Baloo 2 800 overlaid at the bottom.
    - Add the large badge strip for mood and status directly under the photo header (Section 4.5).
    - Use BlurHash for image loading placeholders.
    - Implement the personality section with interactive karma pills.
  </action>
  <verify>
    <automated>grep -q "Baloo 2" templates/public/animal_detail.html && grep -q "badge-" templates/public/animal_detail.html</automated>
  </verify>
  <done>Animal profile header and status badges are updated.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Feeding Timeline and Sighting Gallery</name>
  <files>templates/public/animal_detail.html</files>
  <action>
    - Replace the feeding history table with a vertical timeline using 🍚 icons and volunteer names.
    - Redesign the sighting gallery into a 2-column masonry-lite grid.
    - Apply rounded corners (--radius-lg) and BlurHash to all gallery images.
    - Ensure typography follows the Baloo 2 (headings) / Nunito (body) split.
  </action>
  <verify>
    <automated>grep -q "timeline" templates/public/animal_detail.html && grep -q "🍚" templates/public/animal_detail.html</automated>
  </verify>
  <done>Feeding history and gallery match the Pawprint Studio specification.</done>
</task>

</tasks>

<verification>
1. Visit an Animal Profile page.
2. Verify the animal name is overlaid on a large photo.
3. Check if personality tags respond to taps (Karma pop).
4. Verify the feeding history looks like a timeline, not a table.
</verification>

<success_criteria>
- Profile header is visually striking with rounded, bold typography.
- Status badges are clear and follow the color system.
- Timeline and gallery are mobile-optimized and aesthetically warm.
</success_criteria>

<output>
After completion, create .planning/phases/01-ui-redesign/01-05-SUMMARY.md
</output>
