---
phase: 01-ui-redesign
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [blueprints/public.py, templates/public/index.html]
autonomous: true
requirements: [UI-03]
must_haves:
  truths:
    - "Backend returns hours since last feeding for each sector"
    - "Home page hero uses Baloo 2 and warm greeting"
    - "Quick CTA Strip (Feed/Emergency) is present above the fold"
  artifacts:
    - path: "blueprints/public.py"
      provides: "Updated feeding status data"
    - path: "templates/public/index.html"
      provides: "Redesigned Home Hero"
---

<objective>
Update the backend logic to support the new 3-state feeding status and redesign the Home page hero section to align with Pawprint Studio aesthetics.

Purpose: Enable the visual status grid to show accurate 'hours since fed' and establish the brand tone on the landing page.
Output: Updated index route and redesigned hero section.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@blueprints/public.py
@templates/public/index.html
@design3.md
@RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Sector Feeding Logic in public.py</name>
  <files>blueprints/public.py</files>
  <action>
    - Modify the index() route to calculate the hours elapsed since the last feeding for each sector.
    - Instead of a binary "fed"/"hungry", return a dictionary where keys are sector names and values are the number of hours since the last FeedingLog (or a high number if no logs exist).
    - Ensure time calculations use UTC-aware datetimes.
  </action>
  <verify>
    <automated>python -c "from blueprints.public import index; print('Success if index works')"</automated>
  </verify>
  <done>index route returns quantitative feeding data for sectors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Redesign Home Hero and Quick CTA Strip</name>
  <files>templates/public/index.html</files>
  <behavior>
    - Hero should display: "Hey there, campus friend! 🐾" in Baloo 2 800.
    - Background should be --color-cream.
    - Two large pill buttons (btn-primary for "Log a Feeding", btn-secondary for "Report Emergency") should be visible on mobile.
  </behavior>
  <action>
    - Remove the existing glassmorphism hero.
    - Implement the Hero section as specified in design3.md (Section 6 - Home).
    - Add the "Quick CTA Strip" with the specified pill buttons.
    - Use Tailwind classes like bg-cream, text-ink, font-display, and rounded-pill (defined in Plan 01).
  </action>
  <verify>
    <automated>grep -q "campus friend" templates/public/index.html && grep -q "btn-primary" templates/public/index.html</automated>
  </verify>
  <done>Home Hero and CTAs match the Pawprint Studio design.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Data Exposure | The number of hours since feeding is public information. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Information Disclosure | blueprints/public.py | accept | Only timing metadata is exposed, not PII. |
</threat_model>

<verification>
1. Visit the Home page.
2. Verify the greeting "Hey there, campus friend! 🐾" is prominently displayed.
3. Confirm "Log a Feeding" and "Report Emergency" buttons are visible and styled correctly.
</verification>

<success_criteria>
- Hero section uses Baloo 2 typography and warm palette.
- CTAs are large and tap-friendly.
- Backend provides timing data for the upcoming status grid.
</success_criteria>

<output>
After completion, create .planning/phases/01-ui-redesign/01-02-SUMMARY.md
</output>
