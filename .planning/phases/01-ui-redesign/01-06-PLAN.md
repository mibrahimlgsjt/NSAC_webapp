---
phase: 01-ui-redesign
plan: 06
type: execute
wave: 4
depends_on: [01-01]
files_modified: [templates/public/report.html, templates/auth/login.html]
autonomous: true
requirements: [UI-07, UI-08]
must_haves:
  truths:
    - "Emergency Report page has a soft --color-rosewood-light background"
    - "Severity selector uses large tap-friendly chips instead of a dropdown"
    - "Login page uses the 'Notebook' form styling with ruled-line texture"
  artifacts:
    - path: "templates/public/report.html"
      provides: "Contextual Emergency Reporting"
    - path: "templates/auth/login.html"
      provides: "Notebook-styled Login"
---

<objective>
Redesign the functional views (Emergency Reporting and Login) to align with Pawprint Studio's contextual tone and tactile form design.

Purpose: Reduce friction during urgent reporting and provide a consistent brand experience during authentication.
Output: Updated Emergency Report and Login pages.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@templates/public/report.html
@templates/auth/login.html
@design3.md
@RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Emergency Report with Tone Shift and Chips</name>
  <files>templates/public/report.html</files>
  <action>
    - Set the page background to --color-rosewood-light.
    - Update the header to: "Need help for an animal? 🚨" in Baloo 2.
    - Replace the severity dropdown with the "Severity Chips" (design3.md Section 4.6).
    - Implement the logic to highlight the selected chip using Alpine.js.
    - Update the submit button to .btn-primary with a pulsing ring animation.
  </action>
  <verify>
    <automated>grep -q "severity-chip" templates/public/report.html && grep -q "rosewood-light" templates/public/report.html</automated>
  </verify>
  <done>Emergency report page is visually contextual and tap-friendly.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Notebook Styling to Login Form</name>
  <files>templates/auth/login.html</files>
  <action>
    - Apply the .form-card styling with ruled-line background texture to the login form (Section 4.6).
    - Update input fields to use .form-input with rounded corners and warm borders.
    - Set form labels to use .form-label (Baloo 2 700).
    - Update the "Login" button to use the .btn-primary pill style.
  </action>
  <verify>
    <automated>grep -q "form-card" templates/auth/login.html && grep -q "repeating-linear-gradient" templates/auth/login.html</automated>
  </verify>
  <done>Login page matches the tactile 'notebook' aesthetic.</done>
</task>

</tasks>

<verification>
1. Visit the Emergency Report page and verify the soft red background tint.
2. Tap the "Serious" severity chip and verify it highlights.
3. Visit the Login page and verify the "Notebook" texture in the form card.
</verification>

<success_criteria>
- Emergency page signals urgency via color without causing panic.
- Login form is tactile and matches the brand typography.
- Tap targets meet accessibility standards ( >= 48px).
</success_criteria>

<output>
After completion, create .planning/phases/01-ui-redesign/01-06-SUMMARY.md
</output>
