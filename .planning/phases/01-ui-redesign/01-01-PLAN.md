---
phase: 01-ui-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [templates/base.html]
autonomous: true
requirements: [UI-01, UI-02]
must_haves:
  truths:
    - "Google Fonts Baloo 2 and Nunito are loaded"
    - "Tailwind v4 @theme tokens match design3.md palette"
    - "Body has the dot-grid background texture"
    - "Navigation uses the Floating Pill Bottom Nav design"
  artifacts:
    - path: "templates/base.html"
      provides: "Global styles and layout foundation"
  key_links:
    - from: "templates/base.html"
      to: "Google Fonts"
      via: "link tag"
---

<objective>
Establish the visual foundation for the Pawprint Studio redesign by updating global styles, typography, and navigation in the base template.

Purpose: Ensure all subsequent views have access to the correct design tokens and layout structure.
Output: Updated templates/base.html with v4 theme and floating navigation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@templates/base.html
@design3.md
@RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Typography and Tailwind v4 Theme</name>
  <files>templates/base.html</files>
  <action>
    - Replace Quicksand font import with: https://fonts.googleapis.com/css2?family=Baloo+2:wght@400;600;700;800&family=Nunito:ital,wght@0,400;0,600;1,400&display=swap
    - In the <style> block, implement the Tailwind v4 @theme directive as specified in design3.md (Section 9).
    - Map --color-cream, --color-paper, --color-ink, --color-paw, --color-meadow, etc.
    - Set --font-display to 'Baloo 2' and --font-body to 'Nunito'.
  </action>
  <verify>
    <automated>grep -q "Baloo 2" templates/base.html && grep -q "@theme" templates/base.html</automated>
  </verify>
  <done>Fonts and design tokens are integrated into the base template.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Global Background and Layout Styles</name>
  <files>templates/base.html</files>
  <action>
    - Update the body CSS to include the dot-grid texture from design3.md (Section 7).
    - Set body background-color to var(--color-cream).
    - Update global font-family assignments (Baloo 2 for headings/buttons, Nunito for body).
    - Implement the .badge, .btn-primary, and other global component base styles in the <style> block as per design3.md.
  </action>
  <verify>
    <automated>grep -q "radial-gradient" templates/base.html</automated>
  </verify>
  <done>Global background and typography styles are applied.</done>
</task>

<task type="auto">
  <name>Task 3: Redesign Mobile Navigation to Floating Pill</name>
  <files>templates/base.html</files>
  <action>
    - Replace the existing fixed <nav> with the "Floating Pill Bottom Nav" structure from design3.md (Section 4.3).
    - Use .bottom-nav, .nav-item, and .nav-label classes.
    - Implement the active state logic for icons (Home, Find, SOS, Stock, Me).
    - Ensure the SOS button has the .sos class with the pulsing ring animation.
  </action>
  <verify>
    <automated>grep -q "bottom-nav" templates/base.html && grep -q "nav-item sos" templates/base.html</automated>
  </verify>
  <done>Bottom navigation matches the Pawprint Studio specification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CSS Injection | Theme tokens are static; no user-controlled strings cross into the <style> block. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | templates/base.html | accept | Design tokens are public visual assets; no sensitive data involved. |
</threat_model>

<verification>
1. Open the application in a browser.
2. Verify fonts have changed to the rounded Baloo 2 style.
3. Check the background for the subtle dot-grid pattern.
4. Verify the bottom navigation is a dark floating pill with a pulsing red SOS button.
</verification>

<success_criteria>
- Application uses Baloo 2 and Nunito fonts.
- Background texture is visible.
- Navigation pill is functional and matches the redesign spec.
</success_criteria>

<output>
After completion, create .planning/phases/01-ui-redesign/01-01-SUMMARY.md
</output>
