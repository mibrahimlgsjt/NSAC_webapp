---
phase: 01-ui-redesign
plan: 07
type: execute
wave: 4
depends_on: [01-01]
files_modified: [templates/admin/admin_panel.html]
autonomous: true
requirements: [UI-09]
must_haves:
  truths:
    - "Dashboard panels use the .dashboard-panel style with left-border color accents"
    - "KPI cards use Baloo 2 for large numbers and bold labels"
    - "Alert queue items have a pulsing dot and warm background"
  artifacts:
    - path: "templates/admin/admin_panel.html"
      provides: "Redesigned Admin Dashboard"
---

<objective>
Redesign the Admin Dashboard to maintain data density while incorporating the Pawprint Studio warmth and visual hierarchy.

Purpose: Provide volunteers and admins with a clear, engaging overview of campus animal operations.
Output: Redesigned Admin Dashboard.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@templates/admin/admin_panel.html
@design3.md
@RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Admin Panel Wrappers and KPI Cards</name>
  <files>templates/admin/admin_panel.html</files>
  <action>
    - Wrap the main sections (Medical, Stock, Alerts) in the .dashboard-panel class (design3.md Section 4.7).
    - Add color-coded left borders for each panel (e.g., Lavender for Medical, Sunflower for Stock).
    - Redesign the top KPI cards into a 2x2 grid with Baloo 2 800 for counts and 600 for labels.
    - Ensure panel titles use Baloo 2 and include specified emojis.
  </action>
  <verify>
    <automated>grep -q "dashboard-panel" templates/admin/admin_panel.html && grep -q "border-left-color" templates/admin/admin_panel.html</automated>
  </verify>
  <done>Admin dashboard structure and KPI cards are updated.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Alert Queue and Table Rows</name>
  <files>templates/admin/admin_panel.html</files>
  <action>
    - Style the Alert Queue items as attention cards with --color-rosewood-light background.
    - Add a pulsing dot indicator for new/urgent items.
    - Update table rows to use the .data-row style with rounded corners and hover effects.
    - Implement a floating action button (FAB) or speed-dial in the bottom-right for quick actions (+ Feeding, etc.).
  </action>
  <verify>
    <automated>grep -q "data-row" templates/admin/admin_panel.html && grep -q "rosewood-light" templates/admin/admin_panel.html</automated>
  </verify>
  <done>Admin alerts and data rows match the redesign spec.</done>
</task>

</tasks>

<verification>
1. Log in as an admin and visit the dashboard.
2. Verify the 2x2 KPI grid at the top.
3. Check if panels have colored left-border stripes.
4. Verify "Emergency Alerts" have a soft red background and pulsing indicator.
</verification>

<success_criteria>
- Dashboard is visually organized using Pawprint Studio panels.
- Key metrics are prominent and easy to read.
- Admin interface feels warm and integrated with the public UI.
</success_criteria>

<output>
After completion, create .planning/phases/01-ui-redesign/01-07-SUMMARY.md
</output>
